

************* Витя ************************

_____________________
Подсистема while. Файл while.h.

while < логическое выражение > do оператор


bool ExecWhile()
bool PassWhile()
тут всё стандартно, но опять же не забывай про вложенность циклов.
________________________

Что касается подсистемы repeat
Заголовчный файл подсистемы: repeat.h
В заголовчном файле одна функция:

bool ExecRepeat()

 

Все требования к этой интерфейсной функции описаны в общих требованиях.

Также нужно сделать PassRepeat(), которая аналогична ExecRepeat во всём, кроме одного: она никаких операторов не выполняет, никаких выражений не вычисляет.


!!! Учти, что циклы могут быть вложены друг в друга. Подумай, что нужно сделать, чтобы всё работало корректно. ( Подсказка: в решении будет слово "стек" ).