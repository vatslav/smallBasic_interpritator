1) в случае конца файла или ошибки - завершать логику программы
2)огранечение на имена файлов > 8
3)отладили get_token():
 - распознаются переменные
 - присваивание
 - не сделаны комментарри

4) typeToName - для тестов - возвращает название типа по коду (для визуализации)
5)анализатор завершает нормально работу при ошибки а не висит
6)globals defins сделаны уникальными